# Evidence boundary

The supplied production screenshots were used as the visual target. This directory intentionally does not duplicate them as proof of the native result.

Verified on 2026-07-12:

- Fresh iPhone 17 Pro simulator Debug build succeeded.
- Signed Release device build succeeded.
- `PulseSoc Native Dev` installed and launched on the connected iPhone 16 Pro.
- Device app inventory showed the production `PulseSoc` app and `PulseSoc Native Dev` installed side by side.

No automated physical-device screenshot API was available. Gear tapping, Control Center scrolling, keyboard, permissions, media, calls, Bluetooth, push, lock-screen behavior, and visual screenshot matching remain manual physical-device checks and are not represented as captured proof here.
