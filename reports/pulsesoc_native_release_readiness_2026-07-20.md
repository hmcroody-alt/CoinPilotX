# PulseSoc Native Release Readiness - 2026-07-20

## 2026-07-21 TestFlight Upload Update

Current decision: **BLOCKED for TestFlight upload**.

The repository-side native WebView replacement audit now passes with `0` hard web-exit blockers and `26/26` critical surfaces covered. The remaining blocker is the App Store/TestFlight packaging chain:

- Existing public App Store record: `PulseSoc`, Apple ID `6777591572`, live bundle `com.pulsesoc.app`, live version `1.0`.
- Current native Release target: `com.pulsesoc.nativeapp`, `Apple Development` signing, `aps-environment=development`.
- Local signing identities: Apple Development only; no Apple Distribution identity installed.
- App Store Connect access: not authenticated in this environment; provider, build history, Internal TestFlight groups, IAP state, and agreements were not verifiable.
- EAS project currently resolves to `@hmcroody/pulsesoc-native`, not a verified production App Store provider.
- No archive/export/upload was attempted from this current state because it would be invalid for TestFlight.

Current report: `reports/pulsesoc_native_testflight_upload_2026-07-21.md`.

## Executive Summary

Final result: **NO-GO for replacing the production WebView app today.**

This pass prepared and audited the current native code state on branch `release/undx-nexus-core-v4` while explicitly deferring Apple organization ownership, D-U-N-S verification, Bundle ID migration, App Store Connect ownership, distribution certificates, App Store provisioning, production APNs credentials, archive upload, App Review, and release publishing.

The native app is buildable and testable, but the strict native-only replacement gate fails because current native source still contains production-impacting URL exits, safe web fallbacks, and fallback copy across multiple release surfaces. A simulator launch also exposed a visible Home bottom-dock/composer overlap in the current installed simulator state, so this build must not be uploaded as the final WebView replacement.

## Repository and Branch

| Item | Result |
| --- | --- |
| Repository | `/Users/hmcherie/Desktop/CoinPilotX` |
| Branch | `release/undx-nexus-core-v4` |
| Starting HEAD | `d27fe493 docs(release): continue Apple ownership NO-GO gate` |
| Target native app | `mobile-native` |
| Existing production app preserved | Yes |
| Apple ownership tasks attempted | No |
| App Store upload attempted | No |

## Git Safety

Pre-existing unrelated dirty work was present before this release-prep pass and was preserved. The notable pre-existing dirty areas include auth/login/sign-up screens and session files, native app identity metadata, and brand assets. This report does not claim those changes are release-approved.

Scoped changes made by this pass:

- `mobile-native/src/utils/format.test.ts` - added the existing AsyncStorage Jest mock pattern so the full native Jest suite can run.
- `reports/pulsesoc_native_release_readiness_2026-07-20.md` - this release-readiness report.
- `reports/pulsesoc_native_progress.md` - updated with the current release-prep checkpoint.
- `reports/pulsesoc_native_webview_replacement_readiness.json` - regenerated by the existing strict native-only audit.
- `reports/screenshots/native-release-readiness-2026-07-20/simulator-current-state.png` - simulator visual evidence.

## Automated Verification

| Check | Result | Notes |
| --- | --- | --- |
| `npm ci --prefix mobile-native --no-audit --no-fund --progress=false` | PASS | Dependency install completed; npm emitted deprecation warnings only. |
| `npm run --prefix mobile-native typecheck` | PASS | `tsc --noEmit` completed. |
| `cd mobile-native && EXPO_DOCTOR_ENABLE_DIRECTORY_CHECK=0 npx expo-doctor --verbose` | PASS | 17/17 checks passed. |
| `npm test --prefix mobile-native -- --runInBand --silent` | PASS | 35 suites, 345 tests passed after adding the AsyncStorage mock to `format.test.ts`. |
| `scripts/pulsesoc_native_global_navigation_audit.py` | PASS | Shared header, bottom nav, badges, drawer identity, route dispatchers, Home integration, and reports checked. |
| `scripts/pulsesoc_native_notification_route_parity_audit.py` | PASS | Shared action routes, push registration/revocation routes, deferred routing, and preference categories checked. |
| `scripts/pulsesoc_native_persistent_radio_home_reselect_audit.py` | PASS | Persistent radio/Home reselect audit passed. |
| `scripts/pulsesoc_native_live_webrtc_guest_audio_repair_audit.py` | PASS | Native LiveKit viewer/host audio code-path audit passed. |
| `scripts/pulsesoc_native_calls_audit.py` | PASS | Native calls audit passed. |
| `scripts/pulsesoc_native_music_upload_audit.py` | PASS | Music upload/library audit passed. |
| `scripts/pulsesoc_native_undx_chat_conversation_audit.py` | PASS | UNDX canonical chat route audit passed. |
| `scripts/pulsesoc_native_mission_standard_audit.py` | PASS | Native mission standard audit passed. |
| `scripts/pulsesoc_native_webview_replacement_audit.py` | FAIL | Native-only replacement gate found 57 hard web-exit/fallback blockers across 12 critical surface groups. |
| `scripts/pulsesoc_native_app_foundation_audit.py` | FAIL | Static audit rejects remaining `WebView`/web-fallback references in native source. |
| `scripts/pulsesoc_native_feature_parity_audit.py` | FAIL | Audit report expectations are stale and also flags web dependency assumptions. |
| `scripts/pulsesoc_native_live_audit.py` | FAIL | Older audit expects obsolete `Go Live Web` copy after newer native Live changes. |

## Native-Only Replacement Gate

The strict native-only audit result is **FAIL**.

Machine evidence:

- Native routes discovered: `102`
- Critical surface groups with static native coverage: `14 / 26`
- Hard web-exit blockers: `57`
- Blocker counts: `Linking.openURL=23`, `fallback_status=2`, `safe_web_fallback=4`, `webPath=15`, `web_fallback_copy=28`, `webview_copy=3`

Critical surface groups still blocked or incomplete:

- Authentication
- Search / Discover
- Conversation
- Live
- Camera Studio
- Seller / Store
- Creator Studio
- Courses / Learning
- Events
- Dashboard
- Intelligence / UNDX
- Account / Settings

Representative blocker families:

- Dashboard safe-web fallback routing and `Linking.openURL`
- Notification web target opening
- Terms/privacy fallback entries
- Signup confirmation/help URL exits
- Chat/UNDX result URL opening
- Camera advanced fallback URL opening
- Live fallback copy/error states
- Search/events/course fallback copy
- Premium, account, safety, creator, growth, orders, marketplace, and calls API web exits

Conclusion: the current native app can continue as the parallel native track, but it is not yet eligible for a no-web-redirect production replacement.

## Simulator QA

| Check | Result |
| --- | --- |
| Available simulator inventory | PASS |
| Booted simulator | `PulseSoc iPhone 16 Pro` |
| Native bundle launch | PASS, `com.pulsesoc.nativeapp` launched with pid `87069` |
| Screenshot captured | PASS |
| Screenshot path | `/Users/hmcherie/Desktop/CoinPilotX/reports/screenshots/native-release-readiness-2026-07-20/simulator-current-state.png` |
| Visual readiness | FAIL |

Simulator observation:

- Home opened and loaded production-like content.
- The captured state shows bottom navigation overlapping the Home composer area. This must be repaired before any production replacement claim.

## Physical iPhone QA

| Check | Result |
| --- | --- |
| Connected device | `P3r7or`, iPhone 16 Pro, identifier `F45E640F-6D02-514E-877C-B764E8D6818F` |
| Build identity guard | PASS |
| Physical Release build | PASS |
| Install | PASS |
| Launch | PASS |
| Installed bundle | `com.pulsesoc.nativeapp.dev` |
| Display name | `PulseSoc Native Dev` |
| Production bundle targeted | NO |

The physical iPhone was updated with the current side-by-side native development app using the guarded installer. This is **not** App Store release proof and does not verify hardware-only behavior such as real camera, microphone, Bluetooth routing, lock-screen calls, killed-app push, or background Live/Call behavior.

## System Status

### Authentication

Code-path status: **partially audited**.

The auth/sign-up/session area is currently dirty in the working tree from pre-existing work. Typecheck and Jest pass, including login, signup validation, biometric, and session tests. Release judgment remains blocked until those pending auth changes are reviewed, committed intentionally, and physically verified with a real account.

### Home

Simulator status: **opened, but visual issue observed**.

Home renders, but the current simulator screenshot shows bottom navigation overlapping the composer. This is a release visual blocker. Previous black-cover and call-popup issues are not the subject of this report, but Home cannot be considered frozen for release until this overlap is corrected in the current branch state.

### Messaging / UNDX

Code-path status: **partially ready**.

The UNDX chat audit passes and confirms the canonical native surface uses `ChatScreen`, canonical conversation id `-9001001`, and production routes `/api/pulse-ai/conversation` and `/api/pulse-ai/message`. However, the native-only replacement audit still flags conversation/Chat web exits, including an UNDX result `Linking.openURL` path.

### Calls

Code-path status: **passing static audit**.

Native calls audit passed. Physical two-party audio/video, Bluetooth, lock-screen call UI, killed-app call behavior, and background audio remain physical-device-only release checks.

### Live

Code-path status: **mixed**.

The current Live WebRTC guest/audio repair audit passes and validates native LiveKit token usage, viewer/host media wiring, microphone publication checks, and separation of physical audio validation. An older Live audit fails because it expects obsolete web fallback copy. The strict native-only replacement audit still flags Live fallback copy/error paths.

### Pulse Radio / Music

Code-path status: **passing scoped audits**.

Persistent radio/Home reselect and Music upload/library audits passed. Lock-screen controls, Bluetooth/headset behavior, interruptions, background playback, and one-engine behavior still require physical-device audio QA before App Store readiness.

### Notifications

Code-path status: **passing route audit; production APNs deferred**.

Notification route parity audit passed. Production APNs credentials, production push delivery, locked-device push taps, killed-app launch routing, and App Store provisioning remain deferred/physical-provider checks.

### Navigation

Code-path status: **shared navigation audit passes; native-only routing fails**.

Global navigation audit passed. Strict native-only routing still fails because multiple route/action helpers open web URLs or expose safe web fallbacks.

### Marketplace / Commerce / Creator / Courses / Events / Dashboard

Status: **not ready for no-web replacement**.

These areas remain blocked by native-only audit findings around web exits, safe fallback routing, provider/payment boundaries, or shell-level implementations.

## Security and Privacy

Positive evidence:

- No Apple credentials, App Store Connect credentials, private keys, APNs production credentials, provisioning profiles, IPA, or archive artifacts were committed by this mission.
- Expo Doctor passed.
- SecureStore/biometric/session tests passed.

Remaining release risks:

- Pre-existing auth/session dirty work must be reviewed before release.
- Production App Store signing, distribution entitlements, production APNs entitlement, and App Store privacy metadata are deferred.
- A broad external secret scanner was not run in this pass.

## Performance

Positive evidence:

- Typecheck and full Jest suite passed.
- Prior scoped performance-sensitive work exists for radio, LiveKit, navigation, and list surfaces.

Remaining release risks:

- No current Instruments profile was captured in this pass.
- No current long-scroll/feed/messenger memory profile was captured.
- Physical audio/video performance remains not observed for Live/calls/music.

## Remaining Technical Blockers

True release blockers before replacing the WebView client:

1. Remove or native-route the remaining 57 hard web-exit/fallback blockers.
2. Fix the current Home bottom navigation/composer overlap visible in simulator evidence.
3. Review and intentionally land or reject pre-existing dirty auth/login/sign-up/session changes.
4. Resolve the stale audit expectations or replace old audits with current release gates.
5. Complete authenticated real-account simulator and physical walkthroughs for Home, Messenger, UNDX, Live, Radio/Music, Notifications, Marketplace, Creator, Dashboard, Search, Settings, and account/security flows.
6. Verify physical-device-only behavior: camera, microphone, Bluetooth, lock-screen calls, background calls, killed-app push routing, production APNs, background audio, and real media uploads.
7. Complete native alternatives for commerce/payment/provider areas currently using web exits or safe fallbacks.

## Deferred Apple Tasks

Explicitly deferred and not attempted:

- Apple organization ownership verification
- D-U-N-S verification
- Team ownership verification
- Bundle ID migration to the existing production app identity
- App Store Connect ownership changes
- Distribution certificates
- App Store provisioning profiles
- Production APNs credentials
- Archive upload
- App Store processing
- App Review submission
- Release publishing

## Release Judgment

Can the current native app replace the production WebView app through a normal App Store update today?

**NO.**

Reason: the app is technically buildable and the side-by-side device app was updated, but strict native-only routing fails, simulator visual QA exposed a Home overlap defect, and physical/provider/App Store release checks remain incomplete or explicitly deferred.

## App Store Release Attempt Update - 2026-07-21

Release decision remains **NO-GO**.

Additional evidence from the 2026-07-21 release attempt:

- Public Apple lookup confirms the live PulseSoc App Store app id `6777591572` uses bundle id `com.pulsesoc.app`.
- The native Release archive currently builds bundle id `com.pulsesoc.nativeapp`.
- A local Release archive succeeded, but it is signed with Apple Development, uses a development provisioning profile, includes `aps-environment=development`, includes `get-task-allow=true`, and contains Expo dev-menu/dev-client artifacts.
- No IPA export or App Store upload was attempted because the archive is not App Store-distributable.
- The strict native WebView replacement audit still fails with 54 hard web-exit/fallback blockers.
- The physical iPhone 16 Pro dev sidecar bundle `com.pulsesoc.nativeapp.dev` was built, installed, and launched, but this does not prove production App Store signing or physical feature readiness.

Release reports:

- `reports/pulsesoc_native_app_store_release_attempt_2026-07-21.md`
- `reports/pulsesoc_native_app_store_release_blockers_2026-07-21.json`
- `reports/pulsesoc_native_app_store_release_checklist_2026-07-21.md`

## App Review Critical Cleanup Update - 2026-07-21

Release decision remains **NO-GO**, but the repository-side native replacement web-exit blocker is resolved.

Updated evidence:

- `scripts/pulsesoc_native_webview_replacement_audit.py` now passes.
- Critical static native coverage is `26/26`.
- Hard web-exit blockers are `0`.
- Blocker counts are `{}`.
- Jest passes `38` suites / `369` tests after route tests were updated to assert native-only handling.
- Physical iPhone 16 Pro dev sidecar build/install/launch passed for `com.pulsesoc.nativeapp.dev`.

Remaining release blockers:

- Existing App Store bundle alignment and App Store Connect ownership verification.
- Apple Distribution signing and App Store provisioning.
- Production APNs entitlement/credentials.
- Production-clean App Store target/profile without dev-client/dev-menu artifacts.
- Privacy manifest and App Store Privacy metadata reconciliation.
- Physical production QA for push, camera, microphone, Live, calls, Bluetooth, background behavior, and uploads.

Report:

- `reports/pulsesoc_native_app_review_readiness_2026-07-21.md`
