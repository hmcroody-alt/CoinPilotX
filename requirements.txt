anyio==4.13.0
APScheduler==3.11.2
blinker==1.9.0
boto3>=1.35,<2
certifi==2026.4.22
charset-normalizer==3.4.7
click==8.3.3
cryptography==48.0.0
Flask==3.1.3
firebase-admin>=6.5,<7
gunicorn==23.0.0
google-auth==2.56.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.13
itsdangerous==2.2.0
Jinja2==3.1.6
agora-token-builder==1.0.0
MarkupSafe==3.0.3
# media_service._image_dimensions reads width/height off every uploaded image.
# It was never declared here, so the import failed on every deploy and the
# dimensions of all 318 uploaded images were silently discarded -- see the
# comment on that function.
Pillow>=11,<13
python-telegram-bot==22.7
requests==2.33.1
stripe==15.1.0
SQLAlchemy>=2.0,<3
psycopg2-binary>=2.9
# Signs the ES256 provider token for every APNs push
# (services/pulsesoc_notification_system.py:2561). It was reaching production
# only as a transitive dependency of firebase-admin, which requests
# pyjwt[crypto]>=2.5.0 -- so the day firebase-admin drops or renames it, iOS
# push stops working and says so misleadingly: the import sits inside a
# try/except that returns "APNs credentials are not configured", pointing at the
# environment rather than at the missing package. Same failure shape as the
# Pillow comment above. The floor mirrors firebase-admin's own, so declaring it
# cannot change what pip resolves -- verified with `pip install --dry-run`:
# zero packages installed or upgraded.
PyJWT[crypto]>=2.5,<3
PyYAML>=6.0.2,<7
pywebpush==2.3.0
typing_extensions==4.15.0
tzlocal==5.3.1
urllib3==2.6.3
Werkzeug==3.1.8
