/android/
/ios/Pods/
/ios/build/
/ios/build-*/
/ios/**/*.xcuserdata/
/ios/**/*.xcuserstate
/ios/**/DerivedData/
/.expo/

# Local verification scratch output (npm run verify tee targets)
/.jest-out.txt
/.tsc-out.txt
/.jestcache/

# Staging area for i18n catalog merges. The merged output belongs in
# src/i18n/catalogs/; the per-locale inputs and the merge script are scratch.
/.i18n_tmp/
